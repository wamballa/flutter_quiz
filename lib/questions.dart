class Question {
  late String questionText;
  late bool answer = false;

  // Question(String q, bool a) {
  //   questionText = q;
  //   answer = a;
  // }

  Question(this.questionText, this.answer) {}
  // removed {} so that don't need to specifically state q: a:

}
